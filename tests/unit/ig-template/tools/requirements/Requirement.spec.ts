import {Currency} from "@/ig-template/features/wallet/Currency";
import {CurrencyType} from "@/ig-template/features/wallet/CurrencyType";
import {RequirementProgress} from "@/ig-template/tools/requirements/RequirementProgress";


describe('Requirement', () => {

    test('constructor', () => {
        // Arrange
        const progress = new RequirementProgress(2, 3);
        // Act

        // Assert
    });

});
